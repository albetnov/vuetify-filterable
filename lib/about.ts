// some types declaration if any. If not, delete this file and it's corresponding config at
/**
 * tsconfig.app.json
 * "files": [
 *  "lib/types.ts"
 * ],
 *
 * */

export default function about(message: string) {
  return `About ${message}!`
}
